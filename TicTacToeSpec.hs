-- Test that moving on a board that has already been won does not change the winner

-- Test that moving on a tile actually results in the tile function returning the correct value
-- Make sure this isn't prone to any errors (so always start with an empty board before placing each tile)
